name: Deployment

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: ENV_CONTEXT_VAR
    steps:
      - name: deploy
        # ...deployment-specific steps
